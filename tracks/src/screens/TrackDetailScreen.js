import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const TrackDetailScreen = () => {
  return (
    <Text style={{ fontSize: 50 }}>
      TrackDetail Screen Here
    </Text>
  )
};

const styles = StyleSheet.create({});

export default TrackDetailScreen;
